[project]
name = "pebble"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pebble-tool>=5.0.31",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
